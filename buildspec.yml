version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
   # commands:
    #   - 
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
       - touch /tmp/test && touch /tmp/test22
      # - command
  post_build:
    commands:
       - echo $CODEBUILD_BUILD_SUCCEEDING
       - PL_NUMBER=`echo $CODEBUILD_SOURCE_VERSION | cut -c 4-`
       - echo $PL_NUMBER
       - if [ -z "$CODEBUILD_BUILD_SUCCEEDING" ]; then  "echo 0"; else "echo 1"; fi
artifacts:
  files:
     - /tmp/*
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
